/*
Автор: Булдыгеров А.А.

Задача:
https://ivtipm.github.io/Programming/Glava04/index04.htm#z84 (ctrl + ЛКМ)
4. 84a
Даны натуральное n, действительное х. Вычислить:
$$\sin x+\sin^2\ x +\dots + \sin^n \ x$$

Натуральные числа входят в множество рациональные, которые в свою очередь входят в множество действительных.
*/

//включение модулей (библиотек)
#include <stdio.h>
#include <locale.h>
#include <math.h>

//главная функция программы
int main() 
{
//поддержка кириллицы
    setlocale(LC_ALL, "");

//объявление переменных
int n;
double x;
double sum = 0;

printf("Введите натуральное и действительное число (через пробел): \n");
if (scanf("%d %lf", &n, &x) != 2)
    {
    puts("Введите два числа!");
    return 0;
    }
//цикл от 1 до (n)
/*
for (int i  = 1; i <= n; i++)
//вычисляем sin(x) в степени i и ссумируем
{
    sum += pow(sin(x), i);
}
*/
/* нужно оптимизировать (в цикле есть i++ и после каждого вычисления цикл повторяется)*/

double sinx = sin(x);
double term = sinx;
int i = 1;
while (i <= n)
{
    sum += term;
    term *= sinx;
    i = i + 1; // инкремент внутри тела цикла, а не в заголовке
}
printf("Ответ: %.6lf\n", sum);
return 0;
}